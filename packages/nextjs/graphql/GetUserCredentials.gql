# src/queries/getUserCredentials.graphql
query GetUserCredentials($address: Bytes!) {
  users(where: { address: $address }) {
    credentials(orderBy: Basecamp_id) {
      Basecamp_id
    }
    rank
  }
}
