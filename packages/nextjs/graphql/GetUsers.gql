query GetUsers {
  users(first: 50, orderBy: rank, orderDirection: desc) {
    address
    rank
    blockTimestamp
    credentials(orderBy: Basecamp_id) {
      Basecamp_id
    }
    lastCredentialMinted
  }
}
