query GetUsers {
  users(first: 100, orderBy: rank, orderDirection: desc) {
    address
    rank
    blockTimestamp
    credentials(orderBy: Basecamp_id) {
      Basecamp_id
    }
    lastCredentialMinted
  }
}
